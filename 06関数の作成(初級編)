#関数
関数a <- function(引数){
プログラム本体
}

#繰り返し文
for(変数 in 変数開始値 : 変数終了値){
繰り返し処理される部分
}

##関数の例
t <- 0		#tに計算結果を入れるために初期化
kansu <- function(x){
t <- 0
for(i in 1:x)t<-t+i
list(T=t)
}
kansu(10)	#関数の実行

#比較・判断
if(条件式) "処理1" else "処理2"
ifelse(条件式, "処理1", "処理2")
if(data0110$Rent>50000) "高い", "安い"
